<div class="row">
  <div class="col-auto blogUser">
    <h2>Welcome back, {{name}}!</h2>
  </div>
</div>

{{log logged_in}}

<div class="row mt-4">
  <div class="col-md-6 blogContent">
    <h3>Say what's on your mind in the world of TECH!!:</h3>

    <form class="form new-project-form">
      <div class="form-group">
        <label for="project-name" class="blogTitle">Blog Title:</label>
        <input class="form-input" type="text" id="project-name" name="project-name" />
      </div>
      <div class="form-group">
        <label for="project-desc" class="blogContent">Post:</label>
        <textarea class="form-input" id="project-desc" name="project-desc"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="pinkBtn">Submit Post</button>
      </div>
    </form>
  </div>

  {{#if projects.length}}
  <div class="col-md-6 project-list">
    <h3 class="blogTitle">Blog Posts:</h3>

    {{#each projects as |project|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a class="editBlogLinks" href="/edit/{{project.id}}">{{project.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="pinkBtn" data-id="{{project.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="./js/profile.js"></script>
