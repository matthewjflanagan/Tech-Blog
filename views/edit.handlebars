<div class="text-center signupLoginTitle">
  <h4 class="blogTitle">Blog Title:</h4>
  <input id="nameInput" value={{name}} />
</div>  

<div class="text-center signupLoginTitle">
  <h4 class="blogContent">Post:</h4>
    <input id="editInput" value={{description}} />
</div>
<div class="text-center">
  <p class="blogUser">Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <button id="updateEditBtn" class="pinkBtn" data-id="{{post.id}}">Update Post</button>
  </p>
</div>
